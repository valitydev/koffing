.row
    .col-xs-12
        kof-withdrawal-search-form((onSearch)="onSearch($event)")
.ln_solid
.row
    .col-xs-12
        kof-withdrawal-search-result([withdrawals]="withdrawals")
        kof-stepper((onChange)="onChangePage($event)", [hasNext]="hasNext()", [page]="page + 1")
