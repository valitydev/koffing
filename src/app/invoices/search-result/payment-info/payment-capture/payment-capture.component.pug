doctype html
button.btn.btn-primary.btn-sm(data-toggle="modal", [attr.data-target]="'#' + paymentID + 'capture'")
    | Подтвердить платеж

.modal.fade(tabindex="-1", role="dialog", id="{{paymentID}}capture", data-backdrop="false")
    .modal-dialog.modal-sm
        .modal-content
            .modal-header
                button(class="close", data-dismiss="modal", aria-label="Close")
                    span(aria-hidden="true") &times;
                h5.modal-title Подтвердить платеж?
            .modal-body
                form(name="captureForm", #captureForm="ngForm")
                    label Комментарий:
                    input.form-control([(ngModel)]="reason", name="reason")
            .modal-footer
                .btn-toolbar.pull-right
                    .btn-group.btn-group-sm
                        button.btn.btn-default(data-dismiss="modal") Нет
                    .btn-group.btn-group-sm
                        button.btn.btn-danger(data-dismiss="modal", (click)="capturePayment()") Да
