.x_content
    kof-loading([isLoading]="isLoading")
        table(*ngIf="shops.length").table.table-hover.table-striped
            thead
                tr
                    th ID
                    th Название
                    th.hidden-xs Адрес
                    th Категория
                    th
            tbody(*ngFor="let shop of shops; let i = index")
                tr([ngClass]="{'success': shop.isSuspended, 'danger': shop.isBlocked}", (click)="showDetailsPanel(i)")
                    td {{shop.id}}
                    td {{shop.details.name}}
                    td.hidden-xs {{shop.details.location ? shop.details.location.url : ''}}
                    td {{getCategoryName(shop.categoryID)}}
                    td(*ngIf="!shop.isBlocked")
                        div.pull-right.small-container
                            i.fa.custom-chevron([ngClass]="{'fa-chevron-down': isDetailsPanelVisible(i), 'fa-chevron-right': !isDetailsPanelVisible(i)}")
                    td(*ngIf="shop.isBlocked")
                        span.pull-right Заблокирован
                tr(*ngIf="isDetailsPanelVisible(i)")
                    td(colspan=6)
                        kof-shop-details-panel([shop]="shop", (onChange)="loadData()")
    a.btn.btn-primary.pull-right([routerLink]="['/shops/create']") Создать магазин
