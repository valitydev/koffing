doctype html
button.btn.btn-primary.btn-sm(data-toggle="modal", [attr.data-target]="'#' + invoiceID")
    | Сформировать ссылку на оплату

.modal.fade(tabindex="-1", role="dialog", id="{{invoiceID}}", data-backdrop="false")
    .modal-dialog.modal-lg
        .modal-content
            .modal-header
                button(class="close", data-dismiss="modal", aria-label="Close")
                    span(aria-hidden="true") &times;
                h4.modal-title
                    | Конфигурирование платежной формы
                    a(href="https://rbkmoney.github.io/docs/integrations/checkout/", target="_blank") &nbsp;
                        u Checkout
            .modal-body
                kof-checkout-config-form([form]="checkoutConfigForm")
            .modal-footer
                .form-group.text-left
                    .row
                        .col-xs-12
                            label.text-left
                                | Ссылка на оплату
                        .col-xs-12
                            .input-group
                                input.form-control(type="text", #paymentLinkInput, readonly, [value]="paymentLink")
                                span.input-group-btn
                                    button.btn.btn-sm.btn-primary((click)="copy()")
                                        | Скопировать
