div(*ngIf="datasets.length > 0")
    canvas.revenueChart(
        baseChart,
        chartType="line",
        [datasets]="datasets",
        [labels]="labels",
        [options]="options",
        [colors]="chartColors",
        height="80"
    )
div(*ngIf="datasets.length === 0") Данные отсутствуют