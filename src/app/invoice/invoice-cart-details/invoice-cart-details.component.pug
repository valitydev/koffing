table.table.table-striped
    thead
        tr
            th Стоимость
            th Количество
            th Сумма
            th Налог
            th Описание
    tbody(*ngFor="let lineItem of invoice.cart")
        tr
            td {{lineItem.price | kofRoubleCurrency}} {{invoice.currency | kofCurrency}}
            td {{lineItem.quantity}}
            td {{lineItem.cost | kofRoubleCurrency}} {{invoice.currency | kofCurrency}}
            td {{lineItem?.taxMode?.rate || 'без НДС'}}
            td {{lineItem.product}}